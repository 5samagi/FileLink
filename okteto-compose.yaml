version: "3.8"
services:
  worker:
   build: .
   environment:
    - API_ID=${API_ID}
    - API_HASH=${API_HASH}
    - BOT_TOKEN=${BOT_TOKEN}
    - APP_NAME=${APP_NAME}
    - ARCHIVE_CHANNEL_ID=${ARCHIVE_CHANNEL_ID}
    - Start_Message=${Start_Message}
    - Session_String=${Session_String}
    - Bot_Channel_UserName=${Bot_Channel_UserName}
